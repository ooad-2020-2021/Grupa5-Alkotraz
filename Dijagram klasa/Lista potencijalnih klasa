Lista potencijalnih klasa: 
 
Pjesma (naziv_pjesme (string), zanr_pjesme (string), broj_glasova (int)) 

Narudzba  (list of Artikli, ukupna_cijena(double)) 
 
Meni (ponuda (list of Artikl)) 
 
Artikl (naziv (string), cijena (double), kolicina (int)) 

Stol (id_stola (int), zauzet (bool), broj_osoba(int)) 
 
Rezervacija (br_stola Stol, datum_rezervacije (Date), vrijeme_rezervacije (Time), id_rezervacije (int)) 

Event (event_id (int), naziv (string), datum_eventa (Date), vrijeme_eventa(Time))

Lokal (lokacija (string), lokal_id (int), naziv (string))

Musterija (username (string), email (string))

Uposlenik (username (string), email (string), uposlenik_id (int))

Menadzer (username (string), email (string), lokali (list of Lokal))
